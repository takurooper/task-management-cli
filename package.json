{
  "name": "task-management-cli-web",
  "private": true,
  "version": "0.1.0",
  "description": "Build-time assets for task-management-cli web views",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:process-css": "mkdir -p ./web-src/dist/styles && tailwindcss -c tailwind.config.js -i ./web-src/styles/process.tailwind.css -o ./web-src/dist/styles/process.css --minify"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.3",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.41",
    "preact": "^10.28.4",
    "tailwindcss": "^3.4.10",
    "vite": "^7.3.1"
  }
}
